# Local development uses the Node API automatically when this is absent.
# After the CloudFormation stack is created, set this to its ApiUrl output.
VITE_API_URL=https://your-lambda-id.lambda-url.ap-south-1.on.aws
